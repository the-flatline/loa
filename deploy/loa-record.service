[Unit]
Description=loa record — the recorder consumer (subscribes to the topic, writes records)
After=network.target loa-cortex.service

[Service]
Type=simple
ExecStart=/home/flatline/venv/bin/loa-record
Restart=always
RestartSec=3
User=flatline

[Install]
WantedBy=multi-user.target
